import { describe, expect, test } from '@jest/globals';
//@todo: create tests (see typescript job)
describe('Testing Cart Controller', () => {
  test('POST `/service` route', async () => {
    expect(1).toBe(1);
  });
});
