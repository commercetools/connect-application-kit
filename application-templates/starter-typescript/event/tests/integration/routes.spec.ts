import { describe, expect, test } from '@jest/globals';

describe('Testing Event Controller', () => {
  //@todo: create test see apis in connect-services
  test('POST `/event` route', async () => {
    expect(1).toBe(1);
  });
});
