deployAs:
  - name: service
    applicationType: service
    endpoint: /service
    configurationType:
      PORT: standard
      PROJECT_KEY: secret
      CLIENT_ID: secret
      CLIENT_SECRET: secret
      SCOPE: secret
      REGION : standard
  - name: job
    applicationType: job
    endpoint: /job
    properties:
      schedule: "*/5 * * * *"
    configurationType:
      PROJECT_KEY: secret
      CLIENT_ID: secret
      CLIENT_SECRET: secret
      SCOPE: secret
      REGION : standard
  - name: event
    applicationType: event
    endpoint: /event
    configurationType:
      PROJECT_KEY: secret
      CLIENT_ID: secret
      CLIENT_SECRET: secret
      SCOPE: secret
      REGION : standard
