name: Workflow that runs on pull requests

on: 
  pull_request:
    

jobs:
  lint_and_build:
    runs-on: ubuntu-latest

    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install dependencies, run linter and build package
        uses: ./.github/actions